{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "build",
    "installCommand": "npm install",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "build/server.js"
        }
    ]
}